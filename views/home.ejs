
<%- include("partials/header");-%>

<h1>Home</h1>
<p><%= startingp %></p>
<% posts.forEach(function(post){ %>
    <h1><%= post.title %></h1>
    <p>
        <%= post.content.substring(0, 20)+" ..." %>
        <a hfref="/posts/<%= post.title%>">Read More</a>
    </p>

<% }) %>

<%- include("partials/footer");-%> 